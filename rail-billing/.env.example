# x402 (crypto) rail — Coinbase CDP
CDP_API_KEY_ID=
CDP_API_KEY_SECRET=
X402_PAY_TO=0xYourWalletHere

# Stripe (fiat) rail — OPTIONAL (engine runs x402-only without these)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_SUCCESS_URL=https://example.com/success
STRIPE_CANCEL_URL=https://example.com/cancel

PORT=3000
PUBLIC_URL=http://localhost:3000
