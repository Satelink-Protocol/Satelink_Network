{
  "name": "@satelink/content",
  "version": "0.0.0",
  "private": true,
  "description": "Typed content layer for the public sites: zod schemas for CMS documents + the machine-readable product/pricing contract, a CMS REST client with fallback, and fallback fixtures so builds never fail when the CMS is down.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./schemas": "./src/schemas.ts",
    "./client": "./src/client.ts",
    "./site": "./src/site.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
