// Satelink Signal 2.0 motion primitives — LazyMotion + domAnimation only,
// every one reduced-motion-aware.
export {
  MotionProvider,
  ScrollReveal,
  Stagger,
  StaggerItem,
  CountUp,
  HoverLift,
} from "./primitives";
export type { ScrollRevealProps, StaggerProps, CountUpProps, HoverLiftProps } from "./primitives";
