version: 2
updates:
  # This repo is an npm workspaces monorepo (see root package.json "workspaces").
  # The ONLY lockfile npm/CI reads is the root package-lock.json — never add or
  # commit a nested apps/*/package-lock.json, or Dependabot will target it
  # instead of the root lockfile and every PR it opens will fail `npm ci`.
  # Frontend dependencies - weekly, grouped
  - package-ecosystem: "npm"
    directory: "/apps/web"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "09:00"
    groups:
      react-ecosystem:
        patterns: ["react*", "next*", "@types/react*"]
      ui-components:
        patterns: ["@rainbow-me/*", "wagmi*", "viem*", "recharts*"]
    ignore:
      - dependency-name: "*"
        update-types: ["version-update:semver-patch"]
    open-pull-requests-limit: 3

  # Backend dependencies - weekly, grouped
  - package-ecosystem: "npm"
    directory: "/apps/api"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "09:00"
    groups:
      security-only:
        update-types: ["security"]
    ignore:
      - dependency-name: "*"
        update-types: ["version-update:semver-patch"]
    open-pull-requests-limit: 2

  # GitHub Actions - monthly only
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
    open-pull-requests-limit: 2
