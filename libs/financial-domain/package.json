{
  "name": "@satelink/financial-domain",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Pure financial domain aggregates (ledger). Imports only @satelink/kernel. Zero I/O.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@satelink/kernel": "*"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^3.0.0",
    "fast-check": "^4.0.0"
  }
}
