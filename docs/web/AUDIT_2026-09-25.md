# Production audit — 2026-09-25

Scope: `satelink.network` (/, /pricing, /product/overview, /login), `console.satelink.network`
(unauthenticated → /sign-in), `jakuraa.com` (/). Widths 360 / 390 / 768 / 1024 / 1440, light + dark
(Playwright `colorScheme` + the site's own theme storage: `satelink-theme` localStorage, `slc_theme`
cookie). 60 page captures + 4 mega-menu captures.

- Script: `scripts/audit/prod-screenshot-audit.mjs` (`node scripts/audit/prod-screenshot-audit.mjs <outdir>`)
- Captures + raw measurements: `docs/web/audit-2026-09-25/before/` (`results.json`, `results2.json`)

## Phase 0 start-up checks

| Check | Result |
|---|---|
| `main` pulled, #423 merged | ✅ `b21e996` merged 2026-09-25 04:57 UTC |
| `platform_flags` capacity path (prod, read-only session) | ✅ key is **`capacity_enforcement_path` = `new`** (updated 2026-08-20 by `m9-step5-retry`). There is no key literally named `capacity_path`; this is the only row in `platform_flags`. |
| Dodo test key works (read-only `GET /products`, test mode, via `railway run` on Satelink-api, key never printed) | ✅ HTTP 200, 2 products: `Pro` (recurring, 1900 USD, metadata `satelink_product_id=pro`), `Intelligence credit pack` (one-time, 999 USD, no metadata) |
| Dodo env var **names** on Satelink-api | ❌ Not the names in the brief. Present: `DODO_TESTMODE_API_KEY`, `DODO_WEBHOOK_SECRET_KEY`, `DODO_INTERNAL_SECRET`, `DODO_CREDIT_PACK_PRODUCT_IDS`, `DODO_CREDIT_PACK_USD_VALUES`. **Absent: `DODO_API_KEY_TEST`, `DODO_API_KEY_LIVE`, `DODO_WEBHOOK_SECRET`.** No live key on any Railway service. Code will read `DODO_API_KEY_TEST ?? DODO_TESTMODE_API_KEY` and `DODO_WEBHOOK_SECRET ?? DODO_WEBHOOK_SECRET_KEY`. |
| Admin tokens removed from Railway | ❌ **`ADMIN_SECRET_TOKEN` and `MASTER_ADMIN_TOKEN` are still set on Satelink-api** (production). Not touched (env vars are founder-only). |
| Tokens from local `.env` | `VERCEL_TOKEN`, `CLOUDFLARE_ACCOUNT_ID`, `CLOUDFLARE_ZONE_ID` present; Railway + Vercel CLIs authenticated. |

## Defects

Severity: **S1** broken/unusable · **S2** visibly wrong · **S3** polish.

| ID | Sev | Where | Defect | Evidence | Root cause | Fix (phase) |
|---|---|---|---|---|---|---|
| D1 | S1 | satelink.network, all pages, ≥1024 | **Mega-menu is transparent** and its text collides with the hero eyebrow + H1 | `sat-home-1440-light-megamenu.jpg`; computed `background-color: rgba(0,0,0,0)`, z-index 50 | **No `sl-*` Tailwind utility is generated in production.** `@theme inline { --color-sl-* }` lives in `packages/web-ui/src/styles/tokens.css`, which `app/layout.tsx` imports *separately* from `globals.css`, i.e. outside the `@import "tailwindcss"` compilation. Verified: none of the 3 prod CSS bundles contains `bg-sl-surface` or `--color-sl-surface`. | Import tokens inside the Tailwind entry (§1) |
| D2 | S1 | satelink.network, all pages | **Primary CTAs have no fill** ("Get started", "Start free", "/login → Sign in") — read as ghost buttons with a faint shadow | `sat-home-1440-light.jpg`, `sat-login-1440-light.jpg` | D1 (`bg-sl-accent` missing) | §1 |
| D3 | S2 | satelink.network, all pages | **Every border renders near-black** (header rule, cards, inputs, tabs) instead of `--sl-border` #E1E6EF | all light captures | D1 (`border-sl-border` missing → Tailwind default `currentColor`) | §1 |
| D4 | S2 | satelink.network 1024 | **Header overflows**: "Log in", "Contact sales", "Get started" clipped off the right edge | `sat-home-1024-dark.jpg` | 11 top-level controls (Products, Platform, Solutions, Pricing, Resources, Learn, Search ⌘K, theme, Docs, Log in, Contact sales, Get started) | claude.com header pattern (§1) |
| D5 | S2 | satelink.network ≥1024 | Header crowded — 6 menus + search field + theme + 4 actions | `results.json` `headerItems` | IA | §1: Products · Solutions · Pricing · Developers · Resources · Log in · Try Satelink; search + theme → icon cluster; Contact sales → Solutions + footer |
| D6 | S1 | /product/overview 360 + 390 | **Horizontal page scroll** (scrollWidth − innerWidth = 278 px @360, 248 px @390) | `results2.json` | a fixed-width element in the product selector section | §1 rebuild, gated by no-hscroll test |
| D7 | S2 | /pricing | **Old plan model shown publicly**: "300 Trading-Intelligence calls / month", "2,500 calls", "12,000 calls", "1 / 5 / 20 API keys" | `sat-pricing-1440-light.jpg` | page renders `/v1/plans` (legacy `plans` table) | §4: render from PlanCatalog (Pricing V2) |
| D8 | S2 | /pricing | "RECOMMENDED" badge sits unfilled on top of the Pro card border | `sat-pricing-1440-light.jpg` | D1 | §1 |
| D9 | S1 | console Billing | **Old "included calls / month" plan table** (Free 300 / Pro 2,500 / Max 12,000; API keys 1 / 5 / 20) | code: `apps/console/src/app/(console)/billing/page.tsx` renders `GET /v1/plans`; live `/v1/plans` returns exactly those rows (verified by curl). Not screenshotted: needs a signed-in session. | legacy catalogue | §4: PlanCatalog (UU, session/weekly, Launch) |
| D10 | S1 | console, all data pages | **Console differs per browser** — keys/activity in one browser, empty in another for the same Google account | code: keys held in httpOnly cookie `slc_keys` (`apps/console/src/lib/keys.ts`); no table links Better Auth `user` → `api_credits` (`docs/console/ACCOUNT_LINKING.md`) | per-browser key store | §2 account linking |
| D11 | S2 | console, light theme | Theme cookie `slc_theme=light` sets `data-theme="light"` but the sign-in page still renders the dark palette | `console-390-light.jpg` (`results2.json` dataTheme=light) | console globals only define dark surface values | §1 shared tokens (true light + dark) |
| D12 | S2 | /login vs console /sign-in | Two different sign-in UIs: satelink.network/login is email+**password** in a bordered card; the console is Google + magic link. Neither matches the claude.ai/login pattern | `sat-login-1440-light.jpg`, `console-1440-dark.jpg` | two implementations | §1: one centred sign-in (Google, email, magic link, legal line) |
| D13 | S3 | jakuraa.com | No dark mode — renders the light palette under `prefers-color-scheme: dark` | `jakuraa-390-dark.jpg` (no `data-theme`) | corporate app has no dark tokens | §1 shared system (dark brand layer) |
| D14 | S3 | satelink.network | Wordmark is a bare "S" glyph + "Satelink" in sans; no display serif, so it doesn't share jakuraa.com's typographic standard | all captures | brand layer | §1 |
| D15 | S3 | satelink.network home (dark, 1024) | Hero "machine pays" demo panel is empty at first paint (steps not yet animated), a large blank box | `sat-home-1024-dark.jpg` | animation starts empty | §1: render step 1 statically, animate after |

Not defects (checked): all 30 satelink.network captures and 10 jakuraa.com captures return 200;
no horizontal scroll except D6; the console correctly redirects unauthenticated visitors to /sign-in.

## Not audited here
- Signed-in console pages (would need a real account session in a headless browser; D9/D10 are
  verified from code + live API responses instead).
- docs subdomain (not in the requested scope list).

## Status after Phase 1 (branch `feat/design-system-unified`)

Re-audited against local production builds of the branch (`docs/web/audit-2026-09-25/after/`).

| ID | Status | How |
|---|---|---|
| D1 D2 D3 D8 | **Fixed** | tokens imported inside the Tailwind entry; `sl-*` utilities now generated (verified in the built CSS); guard test |
| D4 D5 | **Fixed** | claude.com header: 5 menus + Log in + Try Satelink; search/theme icon cluster; Contact sales → Solutions + footer. Gate: no control clipped/overlapping at 360–1440 |
| D6 | **Fixed** | `min-w-0` on the overview grid card; no horizontal scroll on any audited page at any width |
| D11 | **Fixed** | real cause: `/sign-in` hard-coded `data-theme="dark"`; it now follows the console theme |
| D12 | **Fixed** | one customer sign-in (console, claude.ai/login pattern); `/login` + `/signup` 307 to it |
| D13 | **Fixed** | jakuraa.com dark brand layer (system preference) |
| D14 | **Fixed** | serif wordmark + display type shared with jakuraa.com |
| D15 | **Fixed** | demo starts on its first line |
| D7 | **Fixed** (branch `feat/web-unified-redesign`) | `/pricing` renders from the PlanCatalog (parity test vs the API's `/v2/plans`); Lighthouse mobile 91–95, a11y 100 |
| D9 | **Fixed** (PR #427) | console Billing renders Pricing V2 from `/v2/plans` |
| D10 | **Fixed** (PR #425) | account-linked keys; three-profile gate |

Also fixed while gating: product-colour text failing AA (links now ink with a coloured icon), unlabelled
calculator input, non-focusable scroll regions (code blocks, tables), `radix-ui` umbrella import
(home first-load JS 278 kB → 172 kB), static font weights → variable fonts.
Lighthouse mobile Performance on `/`: production today **76**; branch **92 / 96 / 95** (3 runs).
